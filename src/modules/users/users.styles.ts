import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    paddingVertical: 10,
  },
  searchContainer: {
    flexDirection: 'row',
    paddingTop: 6,
    gap: 12,
    paddingHorizontal: 10,
  },
});
